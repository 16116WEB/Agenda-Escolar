version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    environment:
      - AGENDA_SECRET_KEY=change-me-in-compose
    command: sh -c "flask --app run.py init-db && python serve.py"
